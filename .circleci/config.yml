version: 2
jobs:
  build:
    docker:
      - image: ubuntuMachine/
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: date
      - run:
          name: building Crosscompiler
          command: |
            pwd 
            cd qt5pibuilder
            bash ./build
